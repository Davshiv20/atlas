source_up

# Local binaries on PATH: `vite`, `tsc`, and `eslint` run without `npx`.
PATH_add "$PWD/node_modules/.bin"

dotenv_if_exists .env
dotenv_if_exists .env.local

if [ ! -d node_modules ]; then
  log_status "no node_modules yet — run: make install"
fi
