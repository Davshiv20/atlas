# Base URL of the engine API (engine/, served by uvicorn).
# Read at build time by most bundlers, so the variable name will need the
# framework's prefix once one is chosen (VITE_, NEXT_PUBLIC_, PUBLIC_, ...).
ATLAS_API_URL=http://localhost:8000
